/* ============
 * Mutation types for the tokens module
 * ============
 *
 * The mutation types that are available
 * on the tokens module.
 */

export const SET_TOKEN_DATA = 'SET_TOKEN_DATA'

export default {
  SET_TOKEN_DATA
}
