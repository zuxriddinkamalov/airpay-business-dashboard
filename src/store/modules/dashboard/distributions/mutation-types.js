/* ============
 * Mutation types for the distributions module
 * ============
 *
 * The mutation types that are available
 * on the distributions module.
 */

export const SET_DISTRIBUTION_DATA = 'SET_DISTRIBUTION_DATA'

export default {
  SET_DISTRIBUTION_DATA
}
